# Neon or local Postgres connection string used by the app and Drizzle scripts.
DATABASE_URL=postgresql://neondb_owner:npg_Ktmb86FPojQq@ep-fragrant-thunder-abrs655s-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
TODOIST_CLIENT_ID=
TODOIST_CLIENT_SECRET=
TODOIST_REDIRECT_URI=
# Dedicated webhook HMAC secret. Falls back to TODOIST_CLIENT_SECRET if absent.
# Generate with: openssl rand -base64 32
TODOIST_WEBHOOK_SECRET=
# AES-256-GCM key for session cookie encryption. Required in production.
# Generate with: openssl rand -hex 32
SESSION_SECRET=
# Structured log level: debug | info | warn | error  (default: info)
LOG_LEVEL=info
